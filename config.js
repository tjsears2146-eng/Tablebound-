/* Public site configuration. NEVER put a service_role, sb_secret or image API key here.
   Empty values open the local demo. Set both values to enable cross-device play. */
window.TABLEBOUND_CONFIG = {
  supabaseUrl: '',
  publishableKey: '',
  functionName: 'table-api',
  pollMs: 2500
};
